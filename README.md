# LiturgX — Masomo ya Kila Siku (Kiswahili)

A Kotlin/Jetpack Compose Android app built around your `lectionary_swahili.json` /
`lectionary_swahili.sql` / `lectionary_swahili.db` bundle, implementing the resolution
algorithm from `APP_LOGIC.md` end to end.

## Opening the project / building

1. Android Studio (Ladybug/2024.2+ or newer) → **Open** → select the `LiturgXApp/` folder.
2. The real Gradle wrapper (`gradlew`, `gradle-wrapper.jar`) is included now, pinned to
   **Gradle 9.3.1** — Android Studio will use it automatically, no manual wrapper generation
   needed.
3. Dependencies are managed via `gradle/libs.versions.toml` (AGP 9.1.1, Kotlin 2.2.10,
   Compose BOM 2024.09.00). The catalog carries the full set of libraries used across your
   other `willykez` projects (Room, Firebase, CameraX, etc.) even though LiturgX itself only
   consumes the Compose/navigation/lifecycle subset today — swap in more as the app grows.
4. Run on a device/emulator with **API 26+**.
5. **CI**: `.github/workflows/release.yml` builds an unsigned release APK on every push to
   `main` and uploads it as a workflow artifact (`LiturgX-unsigned.apk`) — no signing step,
   so wire in your usual autogenerated-keystore signing step if you want this to produce an
   installable release build straight from Actions.
6. `.github/FUNDING.yml` points at `willykez` — update if that's not the right GitHub handle
   for this repo.

Package name: `com.willykez.liturgx`.

## What's implemented (mapped to APP_LOGIC.md)

- **§3 Computus** — `core/Computus.kt`: Meeus/Jones/Butcher Easter algorithm, all anchor
  dates (Ash Wed, Palm Sunday, Triduum, Ascension, Pentecost, Trinity, Corpus Christi,
  Sacred Heart, Advent 1, Christ the King), plus Holy Family / Baptism-of-the-Lord date shifts.
- **§4 Season boundaries** — `core/SeasonResolver.kt`.
- **§5 Period/entry-key resolution** — `core/PeriodResolver.kt`, including the irregular
  shapes: Advent's Dec 17–24 override, Palm Sunday's two Gospels (`_msafara`/`_misa`),
  Easter Vigil's 7 OT readings + per-year epistle/Gospel, Pentecost Vigil's 4-way OT-reading
  picker, Pentecost Day's B/C optional substitutions (exposed via
  `LectionaryRepository.pentecostVigilAlternates()` / `.pentecostDayOptional()` rather than
  auto-swapped, per the doc's explicit instruction).
- **§6 Cycle resolution** — `core/CycleResolver.kt`, extendable past 2035 via the documented
  formula (2026 = Sunday Cycle A is the verified anchor).
- **§7 Precedence** — `data/LectionaryRepository.kt`: skips the fixed calendar entirely during
  Advent Dec 17–24 and all Lent weekdays; applies `sikukuu_maalum` overrides in Ordinary Time;
  surfaces obligatory memorials/feasts as a name+colour change (readings stay seasonal, since
  the dataset has no Common-of-Saints text bank — same fallback the doc recommends for a
  lookup *miss*, applied here on a hit too, for the same reason); optional memorials are
  surfaced as a footnote, never swapped in.
- **§9 Known gaps** — both flagged region settings (`epiphanyMode`, `keepThursdaySolemnities`)
  are real user-facing toggles on the **Mipangilio** (Settings) screen, not hardcoded.

**One documented simplification beyond the spec:** Ordinary Time week-numbering counts
forward from Baptism of the Lord (Block 1) and backward from Christ the King, which is fixed
at week 34 (Block 2) — this matches the official Ordo in the large majority of years, but a
literal per-year Ordo table would be needed for full precision on the rare exception. Flagged
in a code comment in `PeriodResolver.mudaWaKawaida()`.

## The UI

The design idea: the app's whole colour identity *is* today's liturgical colour — the same
way a priest's vestments change with the calendar. Everything (background glow, card borders,
nav bar accents, the little wax-seal badge) derives from a single `LiturgicalColor` and
animates smoothly as you browse. Scripture citations are set in serif type on a near-black,
parchment-toned card — meant to feel like an open missal rather than a generic list app.

- **Leo** (Home) — today, at a glance.
- **Kalenda** — browse any date (day-by-day arrows + a full date picker), same rendering.
- **Watakatifu** — searchable sanctoral calendar.
- **Mipangilio** — the two region toggles.

Citations are tap-to-copy (the dataset stores references, not the passage text itself, so
there's nothing to "expand" — copying makes it one paste away from a Bible app).

## Data

`lectionary_swahili.db` ships in `app/src/main/assets/` and is copied to app-internal storage
on first launch, then queried read-only. The JSON isn't bundled — every shape it described,
including the irregular ones, is already flattened into `readings` per the SQL query examples
in §8, so it wasn't needed for the current feature set.

## Reading engine — citations now expand into full Scripture text

`data/lectionary/CitationParser.kt` parses a citation string (comma/semicolon/dash ranges,
cross-chapter dashes, the Swahili Psalm `+`, and lettered partial verses like `6a`) into ordered
verse references. `data/bible/BibleBooks.kt` normalizes the book name in that citation (Swahili
or English) to the id used by the bundled `bible_swahili.sqlite` (66 books — Deuterocanonical
First Readings like Baruku or Hekima aren't in this edition and fall back gracefully).
`data/bible/BibleRepository.getPassage()` ties the two together against the database, opened via
`data/bible/BibleDatabaseHelper.kt` the same way the lectionary DB is.

On screen, tapping a `ReadingBlock` now resolves and reveals the actual verse text inline
(missal-style serif, with a visible break wherever the citation skips verses) instead of only
copying the reference. Citations that can't be resolved quietly keep the original copy-only
behaviour — no broken "read more" affordance. A new share action on each day's readings
(`data/sharing/DailyReadingShareFormatter.kt`) sends the full text, not just citations, formatted
with the standard Mass-reading responses.

`assets/database/bible_swahili.sqlite` ships alongside the lectionary DB and is copied to
app-internal storage on first use, read-only from then on.
